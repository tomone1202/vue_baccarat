export interface betType {
  col: number;
  text: string;
}

export interface stakeOption {
  text: string;
  val: number;
}

export interface Chip {
  moveParam: {
    x: number;
    y: number;
  };
}
